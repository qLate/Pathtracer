#version 460 core
/// #include "default/_shaderBase.glsl"

layout(local_size_x = 64) in;

int lcp(uint a, uint b) {
    return int(31 - findLSB(a ^ b));
}

void main()
{
    uint index = gl_GlobalInvocationID.x;
    if (index >= nodes.length()) return;
}
